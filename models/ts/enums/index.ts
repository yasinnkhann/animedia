export enum ESearchResultsType {
	MOVIES = 'movies',
	SHOWS = 'shows',
	PEOPLE = 'people',
}

export enum EHorizontalScrollerTimeWindow {
	DAY = 'day',
	WEEK = 'week',
}

export enum EMediaType {
	MOVIE = 'movie',
	SHOW = 'show',
}
