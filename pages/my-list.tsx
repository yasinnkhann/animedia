import React from 'react';

interface Props {}

const MyList = (props: Props) => {
	return (
		<div className='mt-[calc(var(--header-height-mobile)+1rem)]'>my-list</div>
	);
};

export default MyList;
